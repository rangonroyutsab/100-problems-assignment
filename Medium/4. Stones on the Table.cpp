#include <stdio.h>
#include <string.h>

int main()
{
    int n, cnt=0, ans=0, i, j;
    char ch, s[55];
    scanf("%d", &n);
    scanf("%s", s);

    for (i=0; i<n; i++) {
        if (s[i] == 'R') {
            cnt
        }
    }
}
